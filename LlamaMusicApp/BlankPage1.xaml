<Page
    x:Class="LlamaMusicApp.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LlamaMusicApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition  Height="Auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Margin="20" x:Name="Title" Text="Title" FontWeight="Bold" FontSize="15"/>
            <TextBox x:Name="SongTitle_UserInput" PlaceholderText="Enter title" Width="200" HorizontalAlignment="Left" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Margin="20" x:Name="Artist" Text="Artist" FontWeight="Bold" FontSize="15"/>
            <TextBox x:Name="SongArtist_UserInput" PlaceholderText="Enter artist" Width="200" HorizontalAlignment="Left" />

                
                <Button  x:Name="SongSaveButton"
                            Content="Save"
                            Click="SongSaveButton_Click">
                </Button>

        </StackPanel>

        

    </Grid>
    

</Page>
